if A + B > 15. {
    include 'string.file'
    include 'string.file'
} elseif A + B == 15 {
    include 'string.file'
} elseif A == B + 20 {
    include 'string.file'
} else {
    include 'string.file'
}